apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: olab
  namespace: flux-system
spec:
  image: registry.naseyro.com/olab
  interval: 1m
  secretRef:
    name: zot-flux-pull
